function UpdateBadge(tab) {
  console.log('UpdateBadge', tab.url);

  /*if (tab.url.indexOf('github.com') > -1) {
    chrome.browserAction.setBadgeText({tabId: tab.id, text: chrome.i18n.getMessage(BadgeWorking)});
  } else {
    chrome.browserAction.setBadgeText({tabId: tab.id, text: ''});
  }*/
}